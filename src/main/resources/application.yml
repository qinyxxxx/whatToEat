spring:
  datasource:
    url: jdbc:mysql://localhost:3306/whatToEat?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    root: INFO
    com.whatoeat: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %X{traceId} %-5level %logger{36} - %msg%n"

app:
  jwt:
    secret: "_h9W3xZ8LkQ2mRt7VpYf5uN1aG4bSd6cE0JzXo"
    ttl-minutes: 5256000